#include "main.h"
/**
 * _abs - computes absolute value of integer
 * @n: the interger to check
 * Return: the absolute value of interger
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
