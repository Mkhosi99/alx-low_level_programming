#include <stdio.h>

/**
 * main - Entry point
 * @argc: Number of arguments used in main function
 * @argv: Array of pointers to the arguments
 *
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
